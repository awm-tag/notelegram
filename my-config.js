/* ============================================================================
   МОЙ КОНФИГ — AWM // PAY        (это ЕДИНСТВЕННЫЙ файл, который правишь ТЫ)
   ----------------------------------------------------------------------------
   • Это твой личный файл. Он НЕ меняется при обновлениях сайта.
   • Подходит к ЛЮБОЙ версии сайта — просто держи его рядом и не теряй.
   • При новой версии сайта: скопируй ЭТОТ файл поверх — и всё снова работает.
   • Подключается в Сайт.html ПЕРЕД assets/payment-config.js:
        <script src="my-config.js"></script>
        <script src="assets/payment-config.js"></script>
   • Всё, что здесь укажешь, переопределяет значения по умолчанию в сайте.
   • СЕКРЕТНЫЕ ключи (токен бота, QONIX_API_KEY) сюда НЕ пиши — они живут
     ТОЛЬКО на сервере в .env. Здесь только публичные/клиентские значения.
   ========================================================================== */
window.AWM_SETUP = {

  // 1) Адрес, где открывается сайт (для wallet-connect манифеста).
  siteUrl: 'https://notelegram.com',

  // 2) TON-кошелёк, на который приходят все платежи.
  wallet: 'UQCA1IrxpvIR0Xpub1elImv7lF1o5QcPaF6FsEcwf73zhvM4',

  // 3) Ключ tonapi.io — НЕОБЯЗАТЕЛЬНО (читающий, безопасен на статике).
  //    Пусто = без ключа (работает, но лимит ниже). https://tonconsole.com
  tonapiKey: '',

  // 3b) Ключ toncenter.com — НЕОБЯЗАТЕЛЬНО, запасной проверяльщик оплат.
  toncenterKey: '',

  // 4) Адрес твоего backend (VPS). Пусто '' = сайт работает без сервера
  //    (обычный проверяемый TON-перевод). Когда сервер готов — впиши домен.
  //    Сайт шлёт сюда ТОЛЬКО публичные данные заказа (никаких секретов).
  backendUrl: 'https://api.notelegram.com',   // VPS 81.177.212.28

  // 4b) Живой счётчик отзывов (тянется через VPS /reviews/count).
  reviews: { channel: 'awm_otziv', seed: 24361, pollMs: 20000 },

  // 5) Юзернейм Telegram-бота для входа на сайте.
  botUsername: 'awm_pay_bot',

  // 6) Комиссии, %. null = оставить встроенное ценообразование услуги.
  commission: { stars: 17, premium: 17, custom: null, mint: null, boost: null },

};
